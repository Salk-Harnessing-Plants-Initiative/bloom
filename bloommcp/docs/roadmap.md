# bloom-mcp Phase 2 — vertical-slice roadmap

**Status: APPROVED (Elizabeth, 2026-06-15) — adversarially reviewed; tracking issues filed (#305–#309). Implement tier-by-tier via `/new-feature`.**

**Design (source of truth):** vault `docs/superpowers/specs/2026-06-15-bloom-mcp-phase2-design.md` + **`2026-06-15-bloom-mcp-phase2-persistence-design.md`** (persistence/provenance — corrects the IO layer to two ports over the deployed Supabase adapter). Copies in this repo at `bloommcp/docs/`. Master spec: `2026-05-11-metcalf-2026-evelyn-bloom-mcp-design.md`.

**Program:** harden the `bloommcp` prototype into a thin, validated MCP server that **delegates all analysis to `sleap-roots-analyze`**, proven via a **depth vertical slice** (contract pattern end-to-end on 1–2 fast tools). One **OpenSpec change + PR per tier**, TDD, tier-by-tier per the `roadmap-driven-pipeline` workflow.

**How each tier is built:** run the repo's **`/new-feature`** command (staging-first): branch off `origin/staging` → `/openspec:proposal` (`tasks.md` TDD-ordered, **oracle test first**) → `/review-openspec` (reconcile BLOCKING/IMPORTANT literally) → **Elizabeth approval** → `/openspec:apply` (TDD) → `/pre-merge` → PR **to `staging`** (link the tier issue + this roadmap) → `/openspec:archive`. Each tier issue (#305–#309) carries these steps.

**Oracle, generally:** bloom-mcp tools are validated by **reproducing `sleap-roots-analyze`'s golden values *through* the MCP tool** (the #120 wheat-EDPIE fixtures) + the contract's 5 test patterns. Tracking: **one issue per tier** (in `Salk-Harnessing-Plants-Initiative/bloom`).

## Live-state facts (verified 2026-06-15)
- **The delegated functions are ALREADY public** in `sleap-roots-analyze` **v0.1.0a2**: `perform_pca_analysis`, `perform_kmeans_clustering`, `perform_gmm_clustering` are in `__all__`. So Tiers 3–4 are **not** blocked on a new release.
- **The result *types* are in-flight, additive adapters:** `PCAResult` (#149), `HeritabilityResult` (#150), `ClusterResult`/`KMeansResult`/`GMMResult` (#151) are **open PRs** (a reviewed, ready stack) over the dict the `perform_*` functions already return. Consuming them is a **preferred upgrade** (avoids duplicating a ~30-line adapter), **not** a hard gate.
- `sleap-roots-contracts 0.1.0a1` **is** on PyPI (exports `validate_analysis_input` + `canonicalize_role_dtypes`). analyze wires contracts via a PyPI pin in **open PR #155** (not a git ref; not merged).
- **bloommcp today has NO `sleap-roots-analyze` or `sleap-roots-contracts` dependency** (the `source/` files are self-contained vendored sklearn). Tier 0 **introduces** them.
- **bloommcp already reads from AND writes to Supabase Storage as `bloom_agent`** (single `BLOOM_AGENT_KEY`): reads `bloommcp_input/` CSVs (`read_input_csv`) + tables via PostgREST; writes versioned outputs via `AnalysisWriter` → `bloommcp_output/.../v<N>/` + `manifest.json` (RLS migration `20260605000000`). The slice **builds on this behind two ports** (read + write) — it is NOT read-only and NOT CSV-on-disk.
- salk-bloom is **staging-first**: feature work branches off `origin/staging` and PRs target **`staging`**; `main` is the deploy/prod branch (only promotion PRs target it). The bloom agent is **not live**.
- **Benfica reviewed + APPROVED PR #310 (2026-06-15)** and confirmed the legacy `run_X_workflow` tools + `source/*` can be **retired once Stage 1 (Tiers 0–4) lands** (so retirement is *after Stage 1*, not indefinitely deferred), and that `code_versions` should include `sleap-roots-analyze` + `sleap-roots-contracts` versions.

## Dev setup (ready)
`DEV_SETUP.md` + `make init` (mints JWT-signed `BLOOM_AGENT_KEY`) bring up the full stack via `docker-compose.dev.yml` — **bloommcp + langchain-agent + self-hosted Supabase + MinIO + storage-api**. The dev `/health` healthcheck is fixed (probes the **unauthenticated** `/health` custom route in `server.py`; **#125 resolved** by #281 on staging). Two Tier-0/2 preconditions surfaced from the deployed code:
- **Windows: develop inside WSL2 on the native Linux FS** (`~/repos/bloom`), **NOT `/mnt/c/...`** (CRLF/filesystem issues — #124). Onboarding note for Evelyn.
- **`supabase_client.py` raises at import** if `SUPABASE_URL`/`BLOOM_AGENT_KEY` are unset → Tier 0 must make that validation **lazy** so `import bloom_mcp` + fakes-based unit tests run with **no Supabase**.

## Hard constraints / decisions (design)
Thin delegation (re-orchestrate nothing) · small flat fast surface (Qwen3.5-9B + Claude) · flat `src/bloom_mcp/tools/` (no `v1/`, no `generated/manual`) · package-SemVer + stable names (no `_v2`) · **two persistence ports** (`ExperimentReader` read + `ResultStore` write) over the deployed Supabase adapter · **provenance unified with the manifest `VersionEntry`** (schema v2→v3 additive: +`seed`/`agent`/`output_sha256`) · **return links not blobs** · auth/transport unchanged. **Deferred (design §8 + persistence design §8):** URL-namespace, `find_tools`/RAG, async-pipeline tools, api-diff gate, Phase-3 generator, retirement of `source/*` + `run_X_workflow` (**after Stage 1 / Tiers 0–4 — Benfica confirmed on PR #310**), **DB-direct read adapter (sub-project #2), orchestrator-owned/per-user-identity writes + real RLS (Benfica), manifest compare-and-swap (multi-instance)**.

## Tiers

Status: ✅ done · 🔵 in progress · ⬜ not started.

**Progress (2026-07-02):** Tiers **0–3 merged** (Tier 0 [#313](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/pull/313), Tier 1 contract layer, Tier 2 [#323](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/pull/323), Tier 3 `qc_clean` [#356](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/pull/356)); **3b `qc_inspect` ([#366](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/pull/366)) and 4 `pca_analysis` ([#377](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/pull/377)) in flight**; analyze pinned to **`0.1.0a4`** ([#387](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/pull/387)). The status column below reflects this.

| Tier | Goal | Oracle / validation | Depends on | Tracking | Status |
|---|---|---|---|---|---|
| **0 — baseline + test stack** | Restructure `bloommcp/source/`+`tools/`+**`storage/`+`supabase_client.py`** → `src/bloom_mcp/` (uv package); **introduce** deps `sleap-roots-analyze>=0.1.0a2` + `sleap-roots-contracts[pandas]>=0.1.0a1`; add dev-test stack (`pytest`, `hypothesis`, `syrupy`, FastMCP `Client`) + `tests/` layout; **source the #120 turface_19 fixture + recorded golden values into `tests/fixtures/`**; **make `supabase_client` env-validation lazy** (no import-time `RuntimeError`) so imports + fakes-based tests run with no Supabase; decide bloommcp's own `openspec/` scope vs root (`web/openspec` precedent) | `uv` build + `import bloom_mcp` clean **without `SUPABASE_URL`/`BLOOM_AGENT_KEY` set**; existing `server.py` boots + `/health` ok; `uv run pytest` **collects + runs unit suite with no live Supabase** + dev group resolves; full existing suite green; **wheel-import CI smoke** — the built wheel imports in a clean env (`uv build`, [#316](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/316)) | — | [#305](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/305) | ✅ |
| **1 — contract layer** | `src/bloom_mcp/contract/`: `@as_mcp_tool` **— our decorator over FastMCP's `mcp.tool()` (FastMCP = the MCP framework, already a dep; Pydantic for I/O)** (Pydantic I/O validation, exceptions → `BloomMCPError`, **propagates `params.seed` → delegated `random_state=` + records it — does NOT re-seed globally (`sleap-roots-analyze` owns per-estimator seeding)**, `Provenance` **unified with the manifest `VersionEntry`** — schema v2→v3 additive: +`seed`/`agent`/`output_sha256`/logical `key`, and **`code_versions` extended to add `sleap-roots-analyze` + `sleap-roots-contracts`** versions per Benfica) | unit tests: every wrapped call stamps a complete `Provenance` (incl. `seed`) persisted into the manifest; old v2 manifests still read; declared errors → `BloomMCPError`, never raw | Tier 0 | [#306](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/306) | ✅ |
| **2 — persistence layer (read + write ports)** | `src/bloom_mcp/data_access/` (`ExperimentReader` port + `SupabaseReader` adapter [reads `bloommcp_input/` CSVs from Supabase Storage + PostgREST] + `FakeReader`) **and** `src/bloom_mcp/result_store/` (`ResultStore` port + `SupabaseResultStore` wrapping the deployed `AnalysisWriter`/`AnalysisDir` + `FakeResultStore`); repoint existing workflows to the ports; **consolidate the read path onto Supabase `bloommcp_input/`** — retire the legacy local `BLOOM_TRAITS_DIR` path still used by `storage_tools.py` (today dev reads from *both*) | unit tests against the fakes (no live Supabase): `load_experiment(fixture)` returns the expected frame; `ResultStore.create_run`→`commit` records a versioned run + provenance; ports stable so the DB-direct read adapter (sub-project #2) + per-user-identity write adapter drop in unchanged | Tier 0 | [#307](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/307) | ✅ |
| **3 — first granular tool `qc_clean`** (QC foundation; NaN viz follows in Tier 3b) | **ADD** a `qc_clean` tool delegating to analyze's **`clean_traits_for_analysis`** (the minimal-QC entry point — cleanup + validate; [analyze#164](https://github.com/talmolab/sleap-roots-analyze/issues/164)). **NOT** the full 16-step `QCPipeline`, and **no** `load→cleanup→validate` re-stitching in the MCP (that orchestration is analyze's, tested upstream). Reads via `ExperimentReader`; **persists a versioned run via `ResultStore`** = cleaned trait CSV + cleanup log + provenance; returns n-samples/traits in→out summary inline + `resource_link`s. Register in `server.py`; 5 patterns. *Why QC first:* `perform_pca_analysis` silently `dropna()`s (mass, uncontrolled sample loss); the smart cleanup drops bad traits first to **minimize loss** | **Oracle:** on #120 turface_19, produce a **no-NaN** cleaned table **through the MCP tool** with **fewer samples dropped than a naive `dropna()`**; versioned run (cleaned CSV + log) recorded; `tools/list`; schema round-trip + provenance + error-envelope; **MCP holds no QC logic** (delegation only); Claude Desktop | Tiers 1, 2 (**needs `clean_traits_for_analysis` released — analyze#164**; see Tier 3 pre-work) | [#338](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/338) | ✅ |
| **3b — granular tool `qc_inspect`** (NaN/missingness viz at QC time) | **ADD** a **read-only** `qc_inspect` tool that visualizes trait missingness so the agent/user picks cleanup thresholds *before* committing a `qc_clean` run (instead of running blind on defaults). Delegates to analyze's already-public, pipeline-used plotters — **`create_trait_eda_plots`** (per-trait NaN/zero/outlier **fractions with the `qc_clean` threshold lines drawn** + `traits_actually_removed` from the cleanup log) and **`create_exploratory_summary_plots`** → `missing_data_pattern` (sample×trait heatmap), with **`inspect_nan_samples`** for the tabular per-sample report. Reads the **raw** frame via `ExperimentReader` (no `require_clean`); accepts the same threshold params as `qc_clean` so the overlay matches a subsequent clean; returns a summary + per-trait NaN-fraction **recommendation** + `resource_link`s (figures/CSV persisted via `ResultStore`, **or** transient image content — design decision). **No viz logic in the MCP** (delegation only). Register in `server.py`; 5 patterns | **Oracle:** on #120 turface_19, the report flags the two 15.5%-NaN traits and recommends `max_nans_per_trait ≤ 0.15`; **read-only** (writes a report artifact, **not** a `_cleaned.csv` resolvable by `require_clean`); `tools/list`; schema round-trip + provenance + error-envelope; Claude Desktop | Tiers 1, 2, **3 (`qc_clean` #338)**; analyze `>=0.1.0a3` (plotters already public) | [#360](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/360) | 🔵 |
| **3c — granular tool `remove_outliers`** (optional outlier-trim; quality sibling to `qc_inspect`) | **ADD** a `remove_outliers` tool composing **after `qc_clean` (#338)** and **before `pca_analysis` (#308)**: detect + drop outlier samples so PCA/UMAP/clustering can *optionally* run on outlier-trimmed data. Thin delegation to analyze's tested entry points — **`remove_outlier_samples`** (removal; [analyze#165](https://github.com/talmolab/sleap-roots-analyze/issues/165), impl PR [analyze#172](https://github.com/talmolab/sleap-roots-analyze/pull/172) **merged**) + optional **`plot_outlier_analysis`** (plots; [analyze#173](https://github.com/talmolab/sleap-roots-analyze/issues/173), impl PR [analyze#175](https://github.com/talmolab/sleap-roots-analyze/pull/175) **merged**), both **released in `0.1.0a4`** ([analyze#176](https://github.com/talmolab/sleap-roots-analyze/pull/176); bloom pinned via [#387](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/pull/387)). Reads the cleaned run via `ExperimentReader`; **persists a versioned trimmed run via `ResultStore`** (trimmed CSV + report + optional figures) + `resource_link`s. **No outlier/plot logic in the MCP** (delegation only). Register in `server.py`; 5 patterns | **Oracle:** on a cleaned #120 turface_19 fixture, trim the expected outlier samples **through the MCP tool** (matching `remove_outlier_samples`'s report); versioned run recorded; `tools/list`; schema round-trip + provenance + error-envelope; Claude Desktop | Tiers 1, 2, **3 (`qc_clean` #338)**; analyze **`>=0.1.0a4`** (entry points released) | [#378](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/378) | ⬜ |
| **4 — granular tool `pca_analysis`** (consumes `qc_clean`) | **ADD** a `pca_analysis` tool delegating to the already-public `sra.perform_pca_analysis` → `PCAResult`, wrapped by the contract, **consuming the Tier 3 `qc_clean` cleaned run** (by experiment+version, or "latest qc run") and **persisting a versioned run via `ResultStore`** (small numbers inline + `resource_link`s); also **validate-clean its input → `BloomMCPError(assumption_violated, remedy="run qc_clean first")`** if handed un-cleaned data; **register it in `src/bloom_mcp/server.py`**; 5 test patterns. *Leaves the existing `dimred_workflow` + `source/pca.py` in place* (retirement deferred). Prefer consuming upstream `PCAResult` (released in `0.1.0a3`); interim local adapter possible but avoid duplicating it | **Oracle:** reproduce the #120 turface_19 golden PCA (PC1≈86.1%/PC2≈5.8%/PC3≈4.0%) **through the MCP tool** on the `qc_clean` output within tolerance; `tools/list`; schema round-trip + provenance + property + error-envelope; validated on **Claude Desktop** | Tiers 1, 2, **3 (`qc_clean` #338)** (soft: upstream `PCAResult` #149) | [#308](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/308) | 🔵 |
| **5 — granular tool `clustering`** (consumes `qc_clean`) | **ADD** a `clustering` tool delegating to public `sra.perform_kmeans_clustering`/`perform_gmm_clustering` → `KMeansResult`/`GMMResult` (polymorphic), wrapped, **consuming the Tier 3 `qc_clean` cleaned run** (clustering also needs no-NaN numeric input) + **persisting a versioned run via `ResultStore`**; register in `server.py`; 5 patterns + determinism. *Leaves `clustering_workflow` + `source/clustering.py` in place* (retirement deferred) | **Oracle:** determinism (same seed → identical labels) + golden cluster metrics through the tool; appears in `tools/list`; round-trip + provenance + error-envelope; Claude Desktop | Tiers 1, 2, **3 (`qc_clean` #338)** (soft: upstream `ClusterResult`/`KMeansResult`/`GMMResult` #151) | [#309](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/309) | ⬜ |

## Sequencing
Tier order is now **0 → 1 → 2 → 3 `qc_clean` → 3b `qc_inspect` → 3c `remove_outliers` → 4 `pca_analysis` → 5 `clustering`** (QC foundation first; NaN viz in 3b; optional outlier-trim in 3c). `qc_inspect` ([#360](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/360)) and `remove_outliers` ([#378](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/378)) are QC-time companions to `qc_clean` — they inform/refine the cleaned run but do **not** gate Tiers 4–5 (PCA runs fine with outliers). Tiers 0–2 can proceed now. **Tier 3 `qc_clean` needs the minimal-QC entry point released — [analyze#164](https://github.com/talmolab/sleap-roots-analyze/issues/164)**; **Tiers 4–5 consume Tier 3's cleaned output *and* prefer the result-types release** (`PCAResult`/`KMeansResult`, merged to analyze `main` (#149/#150/#151) but unreleased). **Tier 3 pre-work (release):** cut `0.1.0a3` with the result types **+ the minimal-QC entry point** ([analyze#163](https://github.com/talmolab/sleap-roots-analyze/issues/163) + [analyze#164](https://github.com/talmolab/sleap-roots-analyze/issues/164)) → bump bloom-mcp's pin `>=0.1.0a3` ([bloom#327](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/327)).

## Deferred (out of slice scope — triggers in design §8)
**Retirement of `source/*` + the bespoke `run_X_workflow` tools** (**after Stage 1 / Tiers 0–4 — Benfica confirmed on PR #310**; deleting `source/pca.py`/`source/clustering.py` now would break the booting server via the workflow tools' module-level imports — so retire only when the workflow tools are removed/repointed, once the granular tools land) · async/long-running pipeline tools · `find_tools` + RAG-MCP (Phase 3) · URL-namespace versioning · api-diff gate (at first publish) · Phase-3 generator · **DB-direct trait-read adapter** behind `ExperimentReader` (trigger: integration sub-project #2) · **orchestrator-owned / per-user-identity writes + real RLS** behind `ResultStore` (trigger: per-user attribution / least-privilege; Benfica — the single shared `bloom_agent` write identity makes RLS decorative today) · **manifest compare-and-swap** (trigger: bloom-mcp scales past one instance; `AnalysisWriter` has no CAS today).

## Cleanup backlog & #388 (file access) integration (added 2026-07-02)
**Context:** the deployed prototype accumulated legacy paths (local-FS output convention, the `run_X_workflow` tools, `BLOOM_OUTPUT_DIR` overloading). Most of this cleanup is **not** separate effort — it is the *tail of the Stage-1 retirement* already scheduled in Deferred above. [#389](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/pull/389) (opt-in local storage backend, closes [#386](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/386)) introduced a third seam — the `StorageBackend` bytes-destination abstraction, below `ResultStore` — that [#388](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/388) (user file upload/download) will build on.

**Data-access seams (where things plug in):**
- **`ExperimentReader`** (read port) → `SupabaseReader`. *Deferred: DB-direct adapter (sub-project #2).*
- **`ResultStore`** (write port) → `SupabaseResultStore`. *Deferred: per-user-identity writes + real RLS.*
- **`StorageBackend`** (bytes destination, new in #389) → Supabase / Local, sits *below* `ResultStore`.

**Cleanup workstreams (discrete PRs):**

| # | Cleanup | What | Trigger / dep |
|---|---|---|---|
| A | Retire `BLOOM_OUTPUT_DIR` → standardize on `BLOOM_STORAGE_LOCAL_ROOT` | drop from `_REQUIRED_DIRS`; kill dead `o_dir` params; retire legacy un-versioned read fallback | tail of B + F; [#386](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/386) context |
| B | Retire `run_X_workflow` + `source/*` | keystone restructure — removes legacy tool surface + local-dir output convention | **Stage 1 (Tiers 0–5) lands** (Deferred; Benfica-confirmed on #310) |
| C | Consolidate read path | collapse near-dead `_resolve_versioned_cleaned` branches (`version_dir=""`, legacy local read, near-dead `list_prefix` leg) onto `ExperimentReader`/Supabase | Tier 2 scope ([#307](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/307)); independent |
| D | Windows/encoding hardening | `read_text()`-no-encoding `cp1252` bug; POSIX-only atomicity claims; backend-aware boot gate (offline) | surfaced by #389 review; independent |
| F | Outlier suite decision | repoint/retire `plot_outlier_comparison` (reads local `OUTPUT_DIR`); granular replacement is `remove_outliers` ([#378](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/378) / Tier 3c) | blocks A + B completion |

**Through-line — B is the keystone.** When Stage 1 lands, B fires → the local-dir output convention dies → **A and F resolve largely as a byproduct** (straggler local writes in `viz_tools`/`qc_tools` go away; `BLOOM_OUTPUT_DIR` collapses to the local-backend root). **C and D are the two genuinely independent cleanups** and can proceed anytime.

**#388 (file upload/download) integration:**

| #388 part | Builds on | Gated by | Size |
|---|---|---|---|
| 2 — download outputs / signed URLs | `create_signed_url` on the `StorageBackend` seam (#389) | just needs #389 merged; wants C | small |
| 1 — upload inputs via chat | `ExperimentReader` + `bloommcp_input/` consolidation | input consolidation + a provenance decision for ad-hoc CSVs | medium |
| 3 — web + CLI file explorer | `list_prefix`/signed-URL + web/CLI surfaces | **deferred per-user-identity + real RLS** (else the explorer exposes all users' files under the shared `bloom_agent`) | large |

**#388 sequencing:** Part 2 is the near-term win (rides #389's seam, unblocks the "results out" need) → Part 1 after input consolidation → Part 3 only after per-user identity (the deferred `ResultStore` work). Decompose #388 into these three rather than building it as one issue.

**Proposed order:** land #389 (w/ review fixes) → C + D anytime → **#388 Part 2 as a fast follow on the seam** → finish Tiers 3b/4/3c/5 → B fires → A + F resolve → #388 Part 1 → per-user RLS → #388 Part 3.

## Related dependencies (not slice-gating)
- **#116** — the `statistics.py` heritability functions are **already public** (PR **#138 merged**) and `HeritabilityResult` is merged (**#150**); #116 stays open only for a deferred `mypy --strict` item. So a *future* `heritability` tool is **not blocked** by it — it only wants the `0.1.0a3` release (same soft-dep as Tiers 3/4). Not slice-gating.
- analyze↔contracts wiring (PR #155) is **merged**; bloom-mcp depends on `sleap-roots-contracts` **directly**, so this never gated the slice.
- **Tier 3 pre-work (release):** cut analyze **`0.1.0a3`** via the repo's `/prepare-release` command, bundling **(a)** the merged result types ([analyze#163](https://github.com/talmolab/sleap-roots-analyze/issues/163)) **and (b)** the new **minimal-QC entry point `clean_traits_for_analysis`** ([analyze#164](https://github.com/talmolab/sleap-roots-analyze/issues/164), impl in PR #166). The entry point **already defaults to the canonical QC cleanup thresholds** (`max_nans_per_trait=0.2`, `max_nans_per_sample=0.0`) via a temp override in PR #166, so a3 cleans correctly — aligning `apply_data_cleanup_filters`'s own signature defaults ([analyze#167](https://github.com/talmolab/sleap-roots-analyze/issues/167)) is a **deferred cleanliness fix, not a release blocker**. Then bump the bloom-mcp pin `sleap-roots-analyze>=0.1.0a3` + `uv lock` ([bloom#327](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/327)). **`sleap-roots-contracts` needs no new release** (`0.1.0a1` is current). **Outlier removal** — the analyze entry points are **released in `0.1.0a4`**: `remove_outlier_samples` ([analyze#165](https://github.com/talmolab/sleap-roots-analyze/issues/165), PR [analyze#172](https://github.com/talmolab/sleap-roots-analyze/pull/172)) + `plot_outlier_analysis` ([analyze#173](https://github.com/talmolab/sleap-roots-analyze/issues/173), PR [analyze#175](https://github.com/talmolab/sleap-roots-analyze/pull/175)). The bloom-side granular tool is **Tier 3c** ([#378](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/378)), composing after `qc_clean` — optional (PCA runs fine with outliers). Bloom pin bumped to `>=0.1.0a4` ([#387](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/pull/387)).

## Tier 0 sub-issues, publish gate & related threads (filed 2026-06-16 from the #305 PR review)
These refine Tier 0 / the publish milestone — they are **not** new tiers; the 0→4 sequence is unchanged.
- **[#316](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/316) — wheel-import CI smoke** (`uv build` → import in a clean env): formalizes Tier 0's "`uv` build + `import bloom_mcp` clean" oracle as automated CI. **Tier 0 acceptance** (linked in the Tier 0 oracle above).
- **[#315](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/315) — prune vendored analysis deps before PyPI publish** (#305 AC5): strip the now-redundant vendored `source/*` so the published wheel is necessary-and-sufficient. **Pre-publish gate, post-slice** — depends on `source/*` retirement (after Stage 1 / Tiers 0–4); lives at the **publish milestone**, not Tier 0's critical path (cleanup / not-deploy-blocker).
- **[#141](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/141) — numerical-reproducibility smoke** (pinned scientific deps): the CI counterpart of the provenance **environment pointer** (persistence design §4) — catches `numpy`/`scipy`/`sklearn` drift the headline `code_versions` can't. Related to the golden-reproduction oracle (Tiers 3/4).
- **[#311](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/311) / [#222](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/222) — lock CVEs** (cryptography / python-multipart / starlette in the bloommcp `uv.lock`): surfaced by Tier 0's `python-audit` step. Related/security; **not** Evelyn's sequence.

## Tier 3c (`remove_outliers`) review follow-ups (filed 2026-07-08 from the PR #400 review)
`remove_outliers` (Tier 3c / [#378](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/378)) is implemented in PR [#400](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/pull/400) (in review). A 5-lens review surfaced three tracked follow-ups — none block the tool, but the first two are data-integrity items worth an explicit decision:
- **[#419](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/419) — gate persistence on an untrustworthy fit + suggest `isolation_forest`.** The default `mahalanobis` method produces a `very_poor` chi-squared fit on the turface_19 reference fixture yet silently persists the trim as the resolved `require_clean` input for `pca_analysis` (Tier 4). `fit_is_trustworthy=false` is advisory only. Options: gate the commit, don't advance "latest", or require an explicit override; and reconsider `mahalanobis` as the default.
- **[#420](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/420) — order-dependent "latest cleaned" un-trim.** `remove_outliers` shares `tool_class="qc"` + `_cleaned.csv` with `qc_clean`, so a later `qc_clean` silently reverts the trim (no warning); `based_on_version` makes it auditable only after the fact. Cheap interim: a re-trim/superseded warning. Real fix: the dedicated `outliers` tool class + `_resolve_versioned_cleaned` extension (design task 7.2). Relates to cleanup row **F** below.
- **[#421](https://github.com/Salk-Harnessing-Plants-Initiative/bloom/issues/421) — cylinder-scan test fixtures.** The QC/analysis suites (`qc_clean`/`qc_inspect`/`remove_outliers`/`pca_analysis`) pin behaviour only on the small, few-trait `turface_19`; add a representative wide/high-sample **cylinder** fixture (the common workload) so goldens generalize — e.g. mahalanobis covariance conditioning at real trait cardinality. Cross-tier testing improvement, not Tier-3c-specific.

## Reconciliation log (adversarial roadmap review, 2026-06-15)
- **[BLOCKING] Server-break on retirement** (completeness/safety lens): `server.py` registers `dimred_workflow`/`clustering_workflow`, which module-level-import `source.pca`/`source.clustering`; deleting those modules breaks server boot (Tier 0 oracle) and touches Benfica's tools pre-input. → **Reconciled:** Tier 3/4 ADD tools alongside; `source/*` + workflow-tool retirement moved to Deferred (post-Benfica).
- **[BLOCKING] Tier 3/4 not release-gated** (dependency lens): `perform_*` already public in v0.1.0a2; result types are additive. → **Reconciled:** ⛔→⬜, soft dep; "introduce" deps at Tier 0; corrected interim option (no git-ref precedent — analyze used a PyPI pin in PR #155).
- **[BLOCKING] Design line 11 stale** ("result types have landed"): they're open PRs. → **Reconciled:** design doc corrected (separate edit).
- **[IMPORTANT]** result-type names → `ClusterResult`/`KMeansResult`/`GMMResult`; **#116 mis-scoped** (heritability only, not PCA/clustering) → moved to "related, not slice-gating"; **no tool-registration step** → added to Tier 3/4 goals + oracle (`tools/list`); **no test-stack/fixtures step** → added to Tier 0; **Tier 0 is "introduce" not "bump"** deps. All reconciled above.

## Reconciliation log (persistence re-brainstorm, 2026-06-15)
- **Deployed prototype contradicted the read-only / CSV / persistence-moves-up assumptions:** `bloommcp` reads from *and* writes versioned outputs to Supabase Storage as `bloom_agent` (`AnalysisWriter` + `manifest.json`, RLS migration `20260605000000`); the manifest is already a provenance record. → **Reconciled (research-grounded — MCP allows write tools + persist-and-link; MLflow/W&B/Snakemake validate a compute layer owning a versioned artifact store; ports keep it reversible):** build on the deployed writer behind **two ports** (`ExperimentReader` read + `ResultStore` write); **Tier 2 reframed** data-access→persistence; **Tier 0 relocates `storage/`**; **provenance unified** with the manifest (`VersionEntry` v2→v3: +seed/agent/output_sha256); **granular tools persist** via `ResultStore` + return links. Read-only/DB-direct/per-user-identity become **deferred adapters with triggers**. Full design: `2026-06-15-bloom-mcp-phase2-persistence-design.md`.

## Reconciliation log (Benfica review + dev-setup audit, 2026-06-15)
- **Benfica approved PR #310** and resolved two open items: retirement of the legacy `run_X_workflow` + `source/*` is now triggered by **Stage 1 (Tiers 0–4) landing** (not indefinitely deferred); and `code_versions` should add **`sleap-roots-analyze` + `sleap-roots-contracts`** versions → folded into Tier 1 / manifest v3.
- **Dev-setup audit:** the full dev stack (Supabase + MinIO + bloommcp) is ready via `DEV_SETUP.md` + `make init`; **#125** (dev `/health` healthcheck false-unhealthy) is **resolved** by #281 (probes the unauthenticated `/health`). Two preconditions added: **Tier 0** makes `supabase_client` env-validation **lazy** (so imports/tests run with no Supabase); **Tier 2** consolidates the read path onto Supabase `bloommcp_input/` (retire legacy local `BLOOM_TRAITS_DIR`). Windows devs use WSL2 native FS, not `/mnt/c` (#124).
