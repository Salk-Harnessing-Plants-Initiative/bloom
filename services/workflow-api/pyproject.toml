[project]
name = "bloom-workflow-api"
version = "0.1.0"
description = "Public HTTP API to enqueue and track Bloom workflow jobs (video, ...)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.32.0",
]
