[project]
name = "bloom-video-worker"
version = "0.1.0"
description = "Bloom video generation worker service"
requires-python = ">=3.11"
dependencies = [
    "psycopg2-binary>=2.9.0",
    "boto3>=1.40.0",
    "pillow>=12.0.0",
    "numpy>=2.0.0",
]
