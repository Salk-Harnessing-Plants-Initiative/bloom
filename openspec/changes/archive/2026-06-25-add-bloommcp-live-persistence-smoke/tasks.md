## 1. Smoke driver (promote scratch → maintained, with unit-testable logic)

- [x] 1.1 Create `bloommcp/scripts/live_persistence_smoke.py` from the untracked `bloommcp/_live_smoke.py` (create the `bloommcp/scripts/` dir; no `__init__.py` — run as a path, not a module). Add a **module docstring** stating: this runs on the **host** against the dev stack, so it overrides `.env.dev`'s in-container `SUPABASE_URL=http://kong:8000` → host gateway and `/app/...` dirs → host temp dirs, and env MUST be set **before** `import bloom_mcp` because `experiment_utils` caches the dir globals at import. Cross-reference DEV_SETUP.md and the `make bloommcp-smoke` target.
- [x] 1.2 Add the **Tier-0 import-clean** check FIRST, before configuring the live env: `subprocess.run([sys.executable, "-c", "import bloom_mcp; from bloom_mcp.tools import _ports"], env=<copy of os.environ minus SUPABASE_URL and BLOOM_AGENT_KEY>)`; route a non-zero exit through the per-check summary (not a bare assert).
- [x] 1.3 Factor the **pure logic** into importable helpers so it is unit-testable without a live stack: (a) provenance assertions over the run's fields (driven off `get_run()`'s `StoredRun`, not a hand-rolled manifest-entry parser — the manifest is read only for the `manifest_schema_version`); (b) the `output_keys`→download→`sha256`-compare loop (taking an injectable "download bytes" callable); (c) **relational** version-advance detection (`N`→`N+1`, not a hardcoded `v1`→`v2`); (d) a per-check accumulator returning `(summary, exit_code)`. The `main()` wires these to the real `supabase_client` + ports.
- [x] 1.4 Implement the live checks via the helpers: assert manifest **schema version == 3** (the scratch script never did); latest entry has `seed == 42`, `agent == "bloom_agent"` (exact, not truthy), `environment` populated (a non-empty value, asserted by shape not mere truthiness), and non-empty `output_sha256`/`output_keys` over one shared key-set; `sha256(downloaded bytes) == recorded` per artifact. Read the version dir/prefix from the store (`get_run`) rather than hardcoding `bloommcp_output/clustering_turface/`, so the version-advance assertion can't read a stale path.
- [x] 1.5 Replace the manifest-only read-back with the **port** read path: after commit, call `_ports.store().get_run(experiment, tool_class, "latest")` and assert it resolves; run the workflow a **second** time and assert `latest` advances by one version (relationally, `N → N+1`) and that the prior version still resolves. Wrap the read-back in a **bounded retry** (5 attempts, 1s apart, ≤5s ceiling, each attempt logged) to absorb read-after-write lag.
- [x] 1.6 Ensure **every** failure mode — workflow error, import-clean failure, hash mismatch, read-after-write timeout — routes through the per-check summary and `sys.exit(1)` (no path aborts with an unlabelled traceback). Print a clear success line on pass. Delete `bloommcp/_live_smoke.py`.
- [x] 1.7 Add `bloommcp/tests/scripts/test_live_persistence_smoke_logic.py`: unit-test the 1.3 helpers against the existing `fake_supabase_storage` fixture ([bloommcp/tests/conftest.py](bloommcp/tests/conftest.py)) — cover hash-mismatch → recorded FAIL + exit 1; `v1→v2` advance detection; a workflow-error path that still emits the summary before exiting non-zero. Run: `cd bloommcp && uv run --extra test pytest tests/scripts/ -v`.

## 2. `make bloommcp-smoke` target

- [x] 2.1 Add a `.PHONY: bloommcp-smoke` target to `Makefile` with a `##` doc-comment (consistent with `migrate-local`/`check`) and a line in the `help` echo block (help is hand-maintained — a target is invisible in `make help` otherwise). Preconditions (fail fast, actionable, mirror `migrate-local` lines 226-238): dev stack up (`docker ps | grep -q bloommcp` or `db-dev`), `.env.dev` present, `uv` on PATH (`check-uv` prereq).
- [x] 2.2 In the recipe: derive the gateway port from `.env.dev` (`KONG_PORT=$$(sed -n 's/^KONG_HTTP_PORT=//p' .env.dev | head -n1 | tr -d '\r'); KONG_PORT=$${KONG_PORT:-8000}`), export `SUPABASE_URL=http://localhost:$${KONG_PORT}`, and source `BLOOM_AGENT_KEY` from `.env.dev` on an `@`-prefixed line (never echo it, never put it on a command line). Do **not** run `make create-bucket` — the `bloommcp-data` bucket comes from migration `20260605000000` via `make migrate-local`.
- [x] 2.3 Run the driver from `bloommcp/` via `uv run python scripts/live_persistence_smoke.py` (no `--extra test` — `supabase`/`pandas` are main deps; the driver doesn't import pytest).
- [ ] 2.4 Local verify: `make dev-up && make migrate-local && make check && make bloommcp-smoke` — confirm green end-to-end against a real stack. Capture the per-check summary for the PR description. Also confirm the failure path: temporarily corrupt one stored object and confirm the smoke exits non-zero naming the hash check (revert after).

## 3. CI step + regression guard (TDD: guard red → step green, one commit)

- [x] 3.1 **(red)** Create `tests/unit/test_bloommcp_live_smoke_gate.py`. Reuse `_iter_steps` (from `tests/unit/test_ci_workflow_uv_conventions.py`) and `_logical_lines` (from `tests/unit/_workflow_helpers.py`); model on `tests/unit/test_bloommcp_wheel_import_gate.py`. Assert — by step **presence and relative order, never a fixed index** — that the `dev-stack-smoke` job: runs `make migrate-local` **before** `make bloommcp-smoke`; runs `make bloommcp-smoke`; and retains an `if: always()` `docker compose ... down -v` teardown. Emit locating failure messages. Run, confirm red.
- [x] 3.2 **(green)** In `.github/workflows/pr-checks.yml`, add a `make bloommcp-smoke` step to the `dev-stack-smoke` job after `make check`, before the cleanup step. Broaden the existing `if: failure()` debug-logs dump to also print `storage`, `db-dev`, `supabase-minio`, and `kong` logs (the smoke's failure surfaces). Re-run the guard; confirm green. **Commit 3.1 and 3.2 together** so no pushed commit is red (the red→green in 3.1/3.2 is a local checkpoint only).
- [x] 3.3 Confirm the related workflow guards still pass: `tests/unit/test_ci_workflow_uv_conventions.py`, `tests/unit/test_pr_checks_workflow_shape.py`, and `tests/unit/test_ci_dev_stack_smoke.py` (the smoke step adds to that job). The new `uv run` line uses `python`, not `pytest`, so it satisfies the uv-conventions invariants.

## 4. Docs, validate, finalize

- [x] 4.1 Add a bloommcp live-persistence step to `.claude/commands/pre-merge.md` (a **new** bullet under the integration/stack phase — there is no pre-existing #323 "step 6" in this file): `make dev-up && make migrate-local && make check && make bloommcp-smoke` for bloommcp PRs. Add a one-line pointer to `make bloommcp-smoke` under `bloommcp/README.md`'s Development section.
- [x] 4.2 `openspec validate add-bloommcp-live-persistence-smoke --strict` passes.
- [x] 4.3 Green suites: `cd bloommcp && uv run --extra test pytest tests/ -v` (incl. new `tests/scripts/`) and `cd ~/bloom && uv run --extra test pytest tests/unit/ -v`.
- [x] 4.4 Open the PR with **base branch = `egao28/bloommcp-tier2-persistence`** (NOT `staging`) — the adapters + v3 schema exist only there; it cannot merge before #323. Commit plan: (1) `docs(bloommcp):` proposal; (2) `feat(bloommcp):` driver + unit tests + `make bloommcp-smoke` (delete `_live_smoke.py` in this commit); (3) `ci(bloommcp):` smoke step + regression guard (together); (4) `docs(bloommcp):` pre-merge + README pointers.
- [ ] 4.5 **After #323 merges to `staging`**: rebase this branch onto `origin/staging`, re-run the three workflow guards (3.3) + the new gate test, and force-push with `--force-with-lease`. Watch for a `pr-checks.yml` conflict (another change may also add to `dev-stack-smoke`); resolution is mechanical — keep both steps.
