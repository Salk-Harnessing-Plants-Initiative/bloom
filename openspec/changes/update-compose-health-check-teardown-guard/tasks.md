## 1. Commit the revised OpenSpec proposal

- [x] 1.1 Commit the original scope (`docs(#455): openspec proposal — compose-health-check teardown guard`) — done (`20c0469`)
- [x] 1.2 Commit the revision (expanded scope to `Show migration status on failure` + `dev-stack-smoke`'s `Cleanup`; `Migration summary` half converted to a MODIFIED delta) — done (`03e08a4`)

## 2. Add the regression test first (TDD)

- [x] 2.1 Added `test_compose_health_check_teardown_steps_guard_missing_env_ci` to `tests/unit/test_pr_checks_workflow_shape.py` (covers `Migration summary`, `Show migration status on failure`, `Cleanup` — all `.env.ci`) and `test_dev_stack_smoke_cleanup_guards_missing_env_dev` to `tests/unit/test_ci_dev_stack_smoke.py` (covers `dev-stack-smoke`'s `Cleanup` — `.env.dev`), reusing each file's existing job-lookup helpers rather than introducing a third, unified helper module
- [x] 2.2 Ran both new tests against the unpatched workflow and confirmed both FAILED (`AssertionError: ... run: block must guard .env.ci/.env.dev existence as its first line`)

## 3. Guard the four steps in pr-checks.yml

- [x] 3.1 `compose-health-check` → `Migration summary`: added the guard as the first line of the `run:` block; extended the existing "Layer C" comment with the cancellation note (#455)
- [x] 3.2 `compose-health-check` → `Show migration status on failure`: added the guard; extended the existing "Layer B" comment noting it also covers a genuine earlier failure, not just cancellation
- [x] 3.3 `compose-health-check` → `Cleanup`: converted `run:` from a single-line scalar to a `run: |` block; added the guard as its first line plus a new comment (had none before)
- [x] 3.4 `dev-stack-smoke` → `Cleanup`: same conversion + guard + comment, using `.env.dev`
- [x] 3.5 Re-ran both tests from §2 (plus the full `tests/unit/test_pr_checks_workflow_shape.py` and `tests/unit/test_ci_dev_stack_smoke.py` suites, then the full `tests/unit/` suite — 318 passed, 1 skipped) and confirmed all pass
- [x] 3.6 Committed the test additions (§2) together with the workflow fix (§3.1–3.4) as **one** commit rather than two — a standalone test-only commit would leave a red-CI state in history, and the git-workflow review already established this is one inseparable fix: `ci(#455): guard .env.ci/.env.dev-dependent teardown steps against early cancellation/failure` (`c9940df`)

## 4. Validate

- [x] 4.1 Confirmed the four steps are exhaustive for this change's scope: both jobs' `Debug logs on failure` steps (`if: failure()`) share a structurally similar dependency on the same env files but are explicitly out of scope (see proposal.md) — not fixed here
- [x] 4.2 `openspec validate --strict` unavailable in this environment (no `openspec` binary); hand-verified against `openspec/AGENTS.md` format rules instead — also confirmed `pr-checks.yml` still parses as valid YAML and `uvx pre-commit run` (trailing-whitespace, end-of-file, check-yaml, gitleaks) passes on all changed files
- [x] 4.3 Live cancellation path: not artificially triggered (would need throwaway commits, see PR #457) — but PR #457's own `dev-stack-smoke` run hit a genuine unrelated failure (Docker Hub `TLS handshake timeout` pulling the `studio` image during `make dev-up`) with `.env.dev` already generated by `make init`. The guarded `Cleanup` step ran `[ -f .env.dev ] || {...}` as its first line, the check passed (file existed), and `docker compose down -v` completed with no crash — real-world confirmation the guard doesn't break the failure-teardown path. The cancellation and generic-early-failure cases execute the identical guard line, so this is direct evidence for the same code path.
- [x] 4.4 Opened PR #457 (`egao28/compose-health-check-teardown-guard-455` → `staging`, https://github.com/Salk-Harnessing-Plants-Initiative/bloom/pull/457). All checks pass, including `Docker Compose Health Check` (10m19s, all three `.env.ci` guards took the non-skip path) and `Dev stack smoke` (6m47s on re-run after the unrelated flake above).
