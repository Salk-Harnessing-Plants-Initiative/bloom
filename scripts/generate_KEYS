# Deprecated. Local development secret + JWT generation is now runnable:
#
#   make init
#   # or directly:
#   uv run --with pyjwt,python-dotenv python scripts/init_dev.py
#
# That writes a complete .env.dev from .env.dev.example, minting ANON_KEY,
# SERVICE_ROLE_KEY, and BLOOM_AGENT_KEY as JWTs signed by the generated
# JWT_SECRET. For prod/staging/ci secrets, see scripts/generate-secrets.sh.
#
# (This file previously held commented-out `node -e` one-liners that were never
# runnable — see issue #104.)
